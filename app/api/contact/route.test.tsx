import { POST } from "./route"
import { render, screen } from "@testing-library/react"
import {SendMailType} from "@/types/send-mail-type";

describe("Send Email Route", () => {
  it("should return 500 if there is no validation", async () => {
    /**const data: SendMailType = {message: "", title: "", sendFromEmail: "", phoneNumber: ""}
    let request: Request = new Request(data);
    const response = await POST(request)
    const image = await screen.findByTestId(`appStoreImage${appStoreLink}`)
    expect(image).toBeVisible()*/
  })
})